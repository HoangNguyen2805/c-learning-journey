/*
Practice 01 - Goto and Labels

Goal:
Practice the main idea from K&R 3.8 Goto and Labels.

Requirements:

1. Include stdio.h.

2. Create two int variables:
   - row
   - col

3. Create a nested loop:
   - outer loop controls row
   - inner loop controls col

4. Print the row and col being checked.

5. Use an if statement to check for one target location.

6. Use goto to jump to a label when the target is found.

7. Create a label named found.

8. At the label, print the found row and col.

9. Add comments explaining:
   - label
   - goto
   - why goto can be dangerous
   - why goto can escape nested loops
   - why break only leaves one loop

Rules:
- Do not use scanf.
- Do not copy the lesson file exactly.
- Use goto only for this practice.
- In normal beginner code, avoid goto unless there is a strong reason.
*/