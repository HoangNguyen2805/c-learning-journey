#include <stdio.h>

int main() {
    int scores[5];

    scores[0] = 90;
    scores[1] = 80;
    scores[2] = 70;
    scores[3] = 100;
    scores[4] = 85;

    printf("Score 0: %d\n", scores[0]);
    printf("Score 1: %d\n", scores[1]);
    printf("Score 2: %d\n", scores[2]);
    printf("Score 3: %d\n", scores[3]);
    printf("Score 4: %d\n", scores[4]);

    return 0;
}