/*
Practice 1.1.3 — Create Your Own Program

Problem:
Write a complete C program from scratch that prints a custom message.
You decide what to print!

Requirements:
1. Include <stdio.h>
2. Write a main() function
3. Use printf() to print at least 5 lines of your choice
4. Each line must be on its own line (use \n)
5. Add meaningful comments explaining what each printf does
6. Compile and test it

Examples of things you could print:
- A daily affirmation or motivational quote
- Your learning goals for the week
- Steps to solve a problem
- A schedule or checklist
- A creative message

Expected Behavior:
- Program compiles without errors
- Program runs and prints your chosen message
- Output appears on multiple lines

Complexity: O(1) — just printing statements

Key Concept:
- You are now writing complete programs from scratch
- Using printf() with good comments makes code readable
- Each program should have a purpose

EXAMPLE SOLUTION:
    #include <stdio.h>

    int main() {
        printf("My Learning Goals for Today:\n");
        printf("1. Master printf() function\n");
        printf("2. Understand escape sequences\n");
        printf("3. Compile and run without errors\n");
        printf("4. Move on to variables tomorrow\n");
        return 0;
    }
*/

#include <stdio.h>

int main() {
    
    /* TODO: Write your own program here!
       
       Plan:
       1. Decide what message you want to print
       2. Write 5+ printf statements
       3. Make sure each line ends with \n
       4. Add comments explaining what each printf does
       5. Save, compile, and test
       
       Remember:
       - #include <stdio.h> at top
       - int main() { ... }
       - printf() for each line
       - return 0; at the end
       - Semicolon after each printf
    */

    return 0;
}
