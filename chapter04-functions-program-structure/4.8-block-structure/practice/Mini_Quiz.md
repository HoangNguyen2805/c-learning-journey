# Mini Quiz - 4.8 Block Structure

1. What is a block in C?

2. What symbols create a block?

3. Can variables be declared inside a block?

4. Can a variable declared inside an if block be used outside that if block?

5. What does scope mean?

6. When does a block variable stop existing?

7. Can an inner block have a variable with the same name as an outer block?

8. What happens when an inner variable has the same name as an outer variable?

9. Can C functions be defined inside other functions?

10. Why does K&R say C is not fully block-structured?

11. Why is block scope useful?

12. What is the main thing to remember from K&R 4.8?
```
