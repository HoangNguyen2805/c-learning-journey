/*
Practice 01 - Block Structure

Goal:
Practice the main idea from K&R 4.8 Block Structure.

Requirements:

1. In main, create an int variable named score and set it to 90.

2. Print score.

3. Create an if block.

4. Inside the if block:
   - create an int variable named bonus
   - set bonus to 10
   - print bonus
   - print score

5. After the if block:
   - print score again

6. Add a comment explaining why bonus cannot be used outside the if block.

7. Create another block using only braces:

   {
       ...
   }

8. Inside that block:
   - create another variable named score
   - set it to 50
   - print it

9. After that block:
   - print the original score again

10. Add comments explaining:
    - what a block is
    - what scope means
    - why an inner variable can hide an outer variable
    - why functions cannot be defined inside other functions in C

Rules:
- Do not use scanf.
- Do not use arrays.
- Do not use pointers.
- Do not define a function inside main.
*/
```