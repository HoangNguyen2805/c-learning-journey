# Mini Quiz - 4.5 Header Files

1. What is a header file?

2. What file extension does a header file usually use?

3. What kind of things are usually placed in header files?

4. What is a function prototype?

5. Why are prototypes often placed in header files?

6. What is the difference between a `.h` file and a `.c` file?

7. What does `#include` do?

8. What is the difference between these?

```c
#include <stdio.h>
#include "math_utils.h"
```

9. Which one is usually used for standard library headers?

10. Which one is usually used for your own headers?

11. What is a header guard?

12. Why are header guards useful?

13. What does this line mean?

```c
#ifndef MATH_UTILS_H
```

14. What does this line mean?

```c
#define MATH_UTILS_H
```

15. What ends a header guard?

16. Should normal function definitions usually go inside header files?

17. Where should real function definitions usually go?

18. What does `extern` mean in a header file?

19. Should you usually list the `.h` file in the gcc compile command?

20. How do you compile two `.c` files together?

21. What is the main thing to remember from K&R 4.5?
