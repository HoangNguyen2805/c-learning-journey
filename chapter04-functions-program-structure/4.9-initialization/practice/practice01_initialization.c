/*
Practice 01 - Initialization

Goal:
Practice the main ideas from K&R 4.9 Initialization.

Requirements:

1. In main, create and initialize:
   - an int variable
   - a double variable
   - a char variable

2. Print all three variables.

3. Create an int array with 5 elements.

4. Initialize only the first 2 elements.

5. Print all 5 elements.

6. Add a comment explaining why the missing array values become 0.

7. Create a char array using a string literal.

8. Print the char array as a string.

9. Create a function that uses a static local variable.

10. Call that function three times from main.

11. Add comments explaining:
    - what initialization means
    - why local variables should be initialized before use
    - what happens to external variables without initialization
    - what happens to static variables without initialization
    - why static local variables initialize only once
    - how array initialization works
    - why strings end with '\0'

Rules:
- Do not use scanf.
- Do not use pointers directly.
- Do not use structs.
- Do not print uninitialized local variables.
*/