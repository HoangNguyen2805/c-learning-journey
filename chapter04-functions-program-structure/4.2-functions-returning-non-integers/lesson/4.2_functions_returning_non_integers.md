# 4.2 - Functions Returning Non-integers

## Big idea

A function does not have to return `int`.

A function can return other types, such as:

```text id="jeoz4g"
double
float
char
void
```

Example:

```c id="5a6m3j"
double average(double a, double b) {
    return (a + b) / 2.0;
}
```

This function returns a `double`.

---

## Return type

The return type comes before the function name.

```c id="w5bj2h"
double average(double a, double b)
```

Meaning:

```text id="u64s33"
average is a function
it takes two double values
it returns a double value
```

---

## Function prototype must match

If a function returns a non-int type, the prototype is very important.

Prototype:

```c id="9du2qa"
double average(double a, double b);
```

Definition:

```c id="g2qirg"
double average(double a, double b) {
    return (a + b) / 2.0;
}
```

The return type must match:

```text id="vss1dh"
prototype return type = double
function definition return type = double
```

---

## Function returning double

Example:

```c id="yd0b4x"
double celsiusToFahrenheit(double celsius) {
    return celsius * 9.0 / 5.0 + 32.0;
}
```

This returns a decimal value.

---

## Function returning char

A function can return one character.

Example:

```c id="xzsz5y"
char getLetterGrade(int score) {
    if (score >= 90)
        return 'A';
    else if (score >= 80)
        return 'B';
    else if (score >= 70)
        return 'C';
    else
        return 'F';
}
```

This function returns a `char`.

---

## Return expression conversion

The value after `return` is converted to the function return type.

Example:

```c id="3toc24"
double half(int x) {
    return x / 2.0;
}
```

The function returns `double`.

But be careful:

```c id="r1yndc"
int half(double x) {
    return x / 2.0;
}
```

This returns `int`, so the decimal part can be lost.

---

## K&R note about `atof`

K&R uses an example function called `atof`.

```text
atof = ASCII to floating-point
```

`atof` converts TEXT (characters) into a real, usable `double` number.

Example:

```c
#include <stdlib.h>

char text[] = "123.45";
double number = atof(text);
```

Before conversion, `text` is just characters — you cannot do math on it.
After conversion, `number` is a real double — you CAN do math on it.

```text
"123.45" (text)  -- atof() -->  123.45 (double)
```

This matters whenever a number arrives as text instead of being typed
directly into scanf — for example, reading numbers out of a string,
a file, or command-line input (covered properly once we reach arrays
and strings in later chapters).

The important idea right now is just the function-declaration angle:

```text
If a function returns double, C must know that before the function is called.
```

So K&R shows a declaration like:

```c
double atof(char s[]);
```

This tells C:

```text
atof takes a character array
atof returns double
```

(Note: `atof` lives in `<stdlib.h>`, not `<stdio.h>`.)

---

## Old C warning

Older C assumed a function returned `int` if C did not know the function type.

Modern C does not want you to rely on that.

Beginner rule:

```text id="405mil"
Always write function prototypes before main.
```

---

## What I need to remember

```text id="ss58wc"
functions can return more than int

return type comes before function name

double function returns decimal value

char function returns one character

void function returns nothing

prototype must match the function definition

if a function returns double, declare it before calling it

return value can be converted to the return type

wrong return type can lose information

K&R uses atof as an example of a function returning double
```
