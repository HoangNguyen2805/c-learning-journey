```md
# Mini Quiz - 4.7 Register Variables

1. What does `register` suggest to the compiler?

2. Does `register` guarantee that a variable will be faster?

3. Can the compiler ignore `register`?

4. What kind of variables usually use `register`?

5. Can `register` be used for function parameters?

6. Why did older C programs use `register` more often?

7. Why is `register` rarely needed today?

8. Can you use `&` on a register variable?

9. Why can you not take the address of a register variable?

10. What is the main thing to remember from K&R 4.7?
```
