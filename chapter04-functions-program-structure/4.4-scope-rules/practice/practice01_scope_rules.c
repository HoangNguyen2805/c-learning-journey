/*
Practice 01 - Scope Rules

Goal:
Practice the main ideas from K&R 4.4 Scope Rules.

Requirements:

1. Include stdio.h.

2. Create one external int variable:
   - score

3. Write a function:
   - name: printExternalScore
   - parameters: none
   - prints the external score

4. In main:
   - create a local int variable also named score
   - print the local score
   - call printExternalScore

5. Write a function with a parameter:
   - name: printParameter
   - parameter: int score
   - prints the parameter score

6. Add a block inside main:
   - declare a block variable
   - print it inside the block

7. Create one example using extern:
   - write extern int count;
   - define int count later in the file
   - use count in a function

8. Add comments explaining:
   - scope
   - local variable
   - parameter
   - external variable
   - definition
   - extern declaration
   - name hiding
   - block scope

Rules:
- Do not use scanf.
- Do not use arrays yet.
- Do not use pointers yet.
- Do not copy the lesson file exactly.
- Write it from memory first, then check your notes.
*/
```
