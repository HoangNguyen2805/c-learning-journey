# Mini Quiz - 4.6 Static Variables

1. What does `static` mean outside a function?

2. What does `static` mean inside a function?

3. What is an external name?

4. What is an internal name?

5. In K&R calculator, what does `sp` mean?

6. In K&R calculator, what does `val` store?

7. Why should `sp` and `val` be static inside `stack.c`?

8. What functions should other files use instead of directly touching `sp` and `val`?

9. In K&R calculator, what does `buf` store?

10. What does `bufp` track?

11. Why should `buf` and `bufp` be static inside `getch.c`?

12. What functions should other files use instead of directly touching `buf` and `bufp`?

13. What does `getop.c` do?

14. Does `getop.c` directly touch `buf` and `bufp`?

15. Why is it safer to make file data private with `static`?

16. What is the main thing to remember from K&R 4.6?
```
