# Mini Quiz - 4.10 Recursion

1. What does recursion mean?

2. What is a base case?

3. What is a recursive case?

4. Why does a recursive function need a base case?

5. What can happen if recursion never reaches a base case?

6. In `countdown(n)`, why do we call `countdown(n - 1)`?

7. Does each recursive call have its own local variables?

8. What is the call stack?

9. In K&R, what does `printd` do?

10. Why does `printd(n / 10)` happen before printing the last digit?

11. What does factorial mean?

12. What is the base case for factorial?

13. Why is quicksort a recursive idea?

14. What is the main thing to remember from K&R 4.10?
```
