5.3 Quiz:

What is the relationship between an array name and a pointer? Can you reassign an array name?
Explain pointer arithmetic. What does p+1 do if p is an int * vs if p is a char *?
Are arr[i] and *(arr+i) equivalent? Explain why or why not.
When you pass an array to a function, what does the function actually receive?
What does "null-terminated" mean for strings in C?
Write code that uses pointer arithmetic to print the 3rd element of an array.
True or false: "A 2D array can be treated as a 1D array using pointer arithmetic." Explain.
What's the difference between arr and &arr[0]? Do they point to the same address?
Can you do pointer arithmetic on array names? Why or why not?
Explain why functions receiving arrays need the size passed separately.