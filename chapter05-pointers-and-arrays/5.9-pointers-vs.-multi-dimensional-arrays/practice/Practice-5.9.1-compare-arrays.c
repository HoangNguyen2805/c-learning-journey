/*
Practice 5.9.1 — Compare 2D Array vs Pointer Array
Build two data structures and compare them.

Create:
1. Fixed 2D char array: char names[5][20] with 5 names
2. Pointer array: char *names_ptr[5] with same 5 names

Print:
- Memory usage of each structure (sizeof)
- Access pattern (show addresses)
- Time to initialize
- Which is more efficient (memory and clarity)

Names to use:
  "Alice"
  "Bob"
  "Charlie"
  "Diana"
  "Eve"

Output should show:
  2D array size: X bytes (fixed width per row)
  Pointer array size: Y bytes (plus string literals)
  
Key insight: Pointer array saves space, 2D array is simpler for fixed-width data.

DSA: Data structure trade-offs, memory layout analysis.
Complexity: Initialization O(1), space comparison.
*/

#include <stdio.h>
#include <string.h>

int main()
{
    /* Student builds both data structures here */

    return 0;
}
