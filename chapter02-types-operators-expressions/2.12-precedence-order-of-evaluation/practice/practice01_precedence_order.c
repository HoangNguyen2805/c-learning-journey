/*
Practice 01 - Precedence and Order of Evaluation

Goal:
Practice the main ideas from K&R 2.12.

Requirements:

1. Include stdio.h.

2. Create examples showing:
   - multiplication before addition
   - parentheses changing the result
   - division and multiplication with same precedence
   - subtraction grouping left to right

3. Create examples showing:
   - relational operators before logical operators
   - && before ||
   - assignment grouping right to left

4. Print each result.

5. Add comments explaining:
   - precedence
   - associativity
   - parentheses
   - order of evaluation
   - side effects

6. Add comments showing why these are bad:
   - x = i++ + i++;
   - printf("%d %d\n", i, i++);

Rules:
- Do not use scanf.
- Do not use if/else.
- Do not use loops.
- Do not write unsafe side-effect expressions as running code.
- Only show unsafe examples inside comments.
- Do not copy the lesson file exactly.
- Write it from memory first, then check your notes.
*/
```